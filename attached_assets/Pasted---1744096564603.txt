Полностью самостоятельно проверь весь функционал веб-приложения после проведённого рефакторинга. Проверь работу всех реализованных ранее функций и интерфейсов — кнопок, форм, запросов, маршрутов, контроллеров и сервисов.

📌 Что именно нужно сделать:

Проверка работоспособности каждого маршрута (endpoint):
Убедись, что каждый роут возвращает правильные данные и статусы (например, 200, 201, 400, 404, 500).
Убедись, что после рефакторинга ни один из роутов не возвращает ошибки.
Проверка всех форм и кнопок в интерфейсе (Frontend):
Протестируй каждую веб-форму: регистрация, вход, создание учеников, групп, филиалов и секций.
Убедись, что при отправке форм правильно сохраняются данные в БД, а также отображаются уведомления пользователю.
Проверка взаимодействия Frontend-Backend:
Проверь, что после действий пользователя (например, регистрация, подача заявки на пробное занятие) корректно меняются статусы, отправляются уведомления и письма, обновляются записи в БД.
Проверь, что отображение данных на фронтенде после всех действий соответствует ожидаемому результату.
Проверка автоматизации и уведомлений:
Убедись, что правильно работает автоматическое изменение статусов (например, с "пробник" на "зачисленный").
Убедись, что уведомления пользователям отправляются вовремя и с корректной информацией.
Проверка отсутствия повторяющегося кода и ошибок после рефакторинга:
Найди и исправь любые ошибки, появившиеся после разделения кода по модулям.
Проверь отсутствие дублирования логики в коде.
🔧 Что делать в случае выявления ошибок:

Чётко зафиксируй каждую найденную проблему.
Исправь каждую проблему сразу после её обнаружения.
После исправления снова перепроверь затронутую часть функционала.
📝 Результат, который мне нужен:

Подробный отчёт о выполненных проверках, найденных ошибках и способах их устранения.
Полностью рабочее и протестированное приложение, готовое к дальнейшей разработке и добавлению новых функций.
Начинай проверку и дай подробный отчёт о проделанной работе и её результатах.