import { Express } from "express";

export function setupTrialRequestRoutes(app: Express) {
  // Заглушка для маршрутов заявок на пробные занятия
  // Эти маршруты нужно будет перенести из routes.ts
}